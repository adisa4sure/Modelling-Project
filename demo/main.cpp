#include "transformations.hpp"

int main()
{
  // Image image("./images/clean_finger.png"); //for reading images in...
  // Mat dest; //declaring an image
  // image.getmat().convertTo(dest, CV_32FC1, 1./255.); //convertion to [1,0]
  // Mat output = image.getmat().clone(); //cloning a materix image
  // draw_rectangle(image, Point(0,0), 100, 100, 0); //rectangle drawing
  // symmetryx(image, "testx"); //taking symmetry about X-axis
  // Mat black(Size(500,500), CV_8U, Scalar(0)); //creating black matrix
  // Image blackim(black);
  // libfp::transformations::weaken_pressure(blackim, Point(250,250), 200,200, 0.05);
  // libfp::transformations::weaken_pressure(image, Point(127, 160), 250, 280, 0.05);
  // Had to initialize that way or I couldn't access the values
  // Mat destH;
  // // H.convertTo(destH, CV_32FC1, 1./16.);
  // // float cell = destH.at<uchar>(Point(1,1));
  // // cout << "Matrice : " << H  << endl;
  // // Mat ff_in;
  // // image.getmat().convertTo(ff_in, CV_32FC1, 1.0/255.0);
  // // Mat ff = fourier(image.getmat());
  // // image.convolve(H, 1);
  // // dft(image.getmat(), output);
  // // blur(image.getmat(), output, Size(3,3)); //OpenCV function
  // // namedWindow("fourier", WINDOW_AUTOSIZE);
  // // imshow("fourier", ); //display an image
  // // libfp::utils::waitKey(0);
  // // savemat(image.getmat(), "convoluted"); //saving image in png
  // // image.show("convolution"); // display image
  // return(0);
}
